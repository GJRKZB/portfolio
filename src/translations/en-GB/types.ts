export interface translationTypes {
  navigation: {
    techStack: string;
    about: string;
    projects: string;
    contact: string;
  };
  availability: {
    openToWork: string;
    basedIn: string;
  };
  profile: {
    aboutMe: string;
    personalInterests: string;
  };
  footer: {
    cta: string;
    btn: string;
    createdBy: string;
  };
}
